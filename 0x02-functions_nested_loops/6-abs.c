#include <stdio.h>
#include <stdlib.h>
#include "main.h"

/**
 * _abs - the function prototype to find absolute value
 * @a: the function parameter to find its absolute value
 * Description: Write a function that computes
 * the absolute value of an integer
 * Prototype: int _abs(int);
 * Return: Always 0
 */

int _abs(int a)
{
	return (abs(a));
}

#include "main.h"
#include <stdio.h>

/**
 * int add - the function prototype that adds two integers
 * @int: the function parameters to be added
 * Description: Write a function that adds two
 * integers and returns the result.
 * Return: Always 0
 */

int add(int, int)
{
	int addition;

	addition = a + b;

	_putchar('0' + addition);
	return (addition);
}

#include "main.h"
#include <stdio.h>

/**
 * add - the function prototype that adds two integers
 * @a: the first function parameter to be added
 * @b: the second function parameter to be added
 * Description: Write a function that adds two
 * integers and returns the result.
 * Return: Always 0
 */

int add(int a, int b)
{
	return (a + b);
}

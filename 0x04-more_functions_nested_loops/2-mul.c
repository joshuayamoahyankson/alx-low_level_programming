#include "main.h"
#include <stdio.h>

/**
 * mul - the function prototype that multipliers two integers
 * @a: the functions first parameter to be multiplied
 * @b: the functions second parameter to be multiplied
 * Description: Write a function that multiplies two integers.
 * Prototype: int mul(int a, int b);
 * Return: Always 0
 */

int mul(int a, int b)
{
	return (a * b);
}
